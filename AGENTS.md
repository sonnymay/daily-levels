# Daily Levels - Project State

## Product contract

Daily Levels is a deliberately minimal native SwiftUI focus app. Every five minutes of focus earns
one level; the daily class resets at local midnight; the cumulative Hero Collection never resets.
The hero keeps grinding while the phone is locked and sleeps when the user switches apps.

Protect these rules:
- One main screen and one primary Start/Pause/Resume button.
- No feeds, streak pressure, coins, shop, ads, nagging notifications, accounts, backend, or tracking.
- The hero never dies and earned progress is never removed.
- All data stays on device. No third-party dependencies or app-owned network calls.

Read [`SPEC.md`](SPEC.md) before changing product behavior.

## Current release

- App: **Daily Levels**, App ID `6780007939`, bundle `com.santipapmay.DailyLevels`.
- Team `57U5D693VS`; App Store Connect Issuer `69a6de7a-0b32-47e3-e053-5b8c7c11a4d1`.
- Project version: **1.1 build 6**. Build 6 is the first intended free-with-Pro production build.
- Monetization: free through Swordsman; one non-consumable Pro unlock for seven evolutions,
  Knight through Mythic. Product ID `com.santipapmay.DailyLevels.pro`.
- Production customers whose original app build predates build 6 receive Pro automatically through
  verified `AppTransaction` ownership. Never remove this grandfathering path.
- App Privacy remains **Data Not Collected**. Age rating is **9+**.
- Listing copy is in [`AppStore/METADATA.md`](AppStore/METADATA.md); release steps are in
  [`AppStore/SUBMISSION.md`](AppStore/SUBMISSION.md).
- Current opaque release screenshots are in `AppStore/screenshots/release_6_9/` and
  `AppStore/screenshots/release_13_inch/`; upload each set in numeric order.

## Architecture

```text
DailyLevels/
|- FocusEngine.swift       @Observable @MainActor timer, checkpoints, SwiftData persistence
|- LockClassifier.swift    protected-data lock vs app-switch classification
|- FocusLedger.swift       pure per-day aggregation
|- DateUtils.swift         local-midnight interval splitting
|- LevelMath.swift         five-minute level math and level-100 cap
|- KnightClass.swift       ten-class ladder and Pro boundary
|- Store.swift             StoreKit 2 purchase, restore, entitlement, paid-owner migration
|- Models.swift            SwiftData FocusSession and DaySummary
|- Localizable.xcstrings   generated by AppStore/gen_xcstrings.py
`- Views/
   |- MainView.swift            one-screen experience, onboarding, haptics, rating moment
   |- HeroScenePanel.swift      class grind videos and class sleep images
   |- FocusHistoryCard.swift    glanceable seven-day history
   |- HeroCollectionView.swift  cumulative hero journey and Pro entry
   `- PaywallView.swift         one-time unlock with StoreKit-localized price

DailyLevelsTests/
`- 34 unit tests covering level/class math, midnight/DST/timezone handling, cold-launch recovery,
   hero assets, localization invariants, Hero Collection gating, and paid-owner entitlement rules.
```

`FocusEngine` is the source of truth injected through the SwiftUI environment. Completed focus is
stored as local `FocusSession` segments. The one-second timer updates display state; wall-clock
intervals, rather than tick counts, determine earned time.

While grinding, the engine checkpoints at every earned level and local-day change. A foreground
crash loses only the unproven remainder. Once a lock is confirmed, a persisted marker allows the
locked interval to recover after system termination, capped at one full daily climb. App-switch time
is never saved.

## Build and test

```bash
SIM=C494865D-5987-4B80-A5D4-EE9EAD88FAA5
xcodebuild -project DailyLevels.xcodeproj -scheme DailyLevels \
  -destination "id=$SIM" CODE_SIGNING_ALLOWED=NO test
```

Last verified July 10, 2026: simulator build succeeded and **34/34 tests passed**. Debug screenshot
flags: `-seedDemoData -autoStart -todayMinutes N -unlockPro`.

## Release gates

- Lock-vs-app-switch behavior still needs the physical passcode-enabled iPhone checks in
  [`TESTING.md`](TESTING.md). Simulator tests cannot close this gate.
- Sandbox-test purchase, restore, and the paid-1.0 upgrade before release.
- Use only StoreKit's localized `displayPrice`; never hardcode a storefront price.
- Never commit App Store Connect `.p8` keys.
- Stop before final App Store submission or release until the owner explicitly confirms it.

## Out of scope

Tabs, a full history screen, settings, streaks, goals, loot, coins, HP, a shop, social features,
accounts, sync, notifications, widgets, Live Activities, and Android. Do not add them without a new
explicit product decision.
